import React from 'react'

const Alert = (props) => {
    return (
        <div className="alert alert-primary" role="alert">
            This is an ongoing website
        </div>
    )
}

export default Alert
